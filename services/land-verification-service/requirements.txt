fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
# PDF report generation (the report generator falls back to a built-in
# minimal PDF writer when reportlab is not installed)
reportlab==4.2.5
# PostgreSQL persistence driver; required when DATABASE_URL points at postgres
# (without it the service persists to a local SQLite file).
psycopg[binary]==3.2.3
