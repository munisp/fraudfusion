fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
httpx==0.28.1
# Multipart form parsing for /biometric/*/upload + /document/* file uploads.
python-multipart==0.0.20
# PostgreSQL persistence; without DATABASE_URL the service uses a local
# SQLite file (stdlib sqlite3) for local dev/tests.
psycopg[binary]==3.2.3
